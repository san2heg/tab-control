* Add: Make TAB_LIMIT persist after chrome closes (try to use options, not storage)
- Bug: Tab limit can be lower than current amount of tabs
- Add: Option to ignore pinned tabs (defaults to yes)
- Make UI pretty
- Deal with 
